import UI

if __name__ == "__main__":
    entry = UI.InterfaceManager()  # 创建完就自己跑了

    # 先保存一个csv
    #data = pd.DataFrame()
    #data['主播'] = ["猫芒"]
    #data['房间号'] = ["21811136"]
    #data.to_csv('./一些直播间.csv', sep=',', index=False, header=True, encoding="utf_8_sig")